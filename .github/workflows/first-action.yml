name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      -  name: Print greeting
         run: echo "hello world"
      -  name: Print goodbye
         run: |
            echo "good bye"
            echo "au revoir"
         
